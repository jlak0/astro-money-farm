---
import { BlogPostPage, getBlogPostStaticPaths } from '@astro-money-farm/core/pages';
import siteConfig from '../../config';

export const getStaticPaths = getBlogPostStaticPaths;

const { post } = Astro.props;
---

<BlogPostPage config={siteConfig} post={post} />
