---
import { BlogCategoryPage, getBlogCategoryStaticPaths } from '@astro-money-farm/core/pages';
import siteConfig from '../../../config';

export const getStaticPaths = getBlogCategoryStaticPaths;

const { category, posts } = Astro.props;
---

<BlogCategoryPage config={siteConfig} category={category} posts={posts} />
